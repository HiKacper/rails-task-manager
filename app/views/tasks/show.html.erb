<h2><%= @task.title %></h2>
<p> <%= link_to "Edit the task", edit_path(@task) %></p>
<p><%= @task.details %></p>
<p><%= @task.completed %></p>
<p> <%= link_to "Get back to tasks", tasks_path %></p>
<%= link_to "Delete",
  task_path(@task),
  data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}
%>
